name: Debug - categoria do lote Ethnographia Brazileira

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Baixar os arquivos do repositório
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Preparar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalar dependências
        run: pip install requests beautifulsoup4 playwright

      - name: Rodar diagnóstico
        run: python scripts_debug_categoria3.py > debug_categoria3_output.txt 2>&1

      - name: Publicar resultado
        run: |
          git config user.name "monitor-bot"
          git config user.email "monitor-bot@users.noreply.github.com"
          git add debug_categoria3_output.txt
          git commit -m "Debug: testar busca por nome completo do autor com/sem tp=" || echo "nada novo"
          git pull --rebase --autostash origin main
          git push
