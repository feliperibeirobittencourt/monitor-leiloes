name: Debug - busca por autor Sílvio Romero

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Baixar os arquivos do repositório
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Preparar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalar dependências
        run: |
          pip install requests beautifulsoup4 playwright
          playwright install --with-deps chromium

      - name: Rodar diagnóstico
        run: python scripts_debug_romero.py

      - name: Publicar resultado
        run: |
          git config user.name "monitor-bot"
          git config user.email "monitor-bot@users.noreply.github.com"
          git add debug_romero_output.txt
          git commit -m "Debug: busca por Sílvio Romero" || echo "nada novo"
          git pull --rebase --autostash origin main
          git push
